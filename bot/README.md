# IES CRM Telegram Bot

Telegram-бот для управления товарами в системе IES CRM.

## Возможности

- 🔐 Авторизация через email и пароль
- 📦 Просмотр списка товаров
- ➕ Добавление новых товаров
- ✏️ Редактирование существующих товаров
- 🗑️ Удаление товаров
- ➕ Приход товаров (увеличение количества)
- ➖ Расход товаров (уменьшение количества)

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните переменные окружения в `.env`:
   - `TELEGRAM_BOT_TOKEN` - токен бота от [@BotFather](https://t.me/BotFather)
   - `API_URL` - URL вашего Laravel API (например, `http://localhost/api`)

## Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

## Запуск

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Продакшн режим (обычный запуск):
```bash
npm start
```

### Запуск с PM2 (рекомендуется для сервера):

PM2 позволяет запустить бота как фоновый процесс, который будет работать постоянно, даже после закрытия терминала.

1. Установите PM2 глобально (если еще не установлен):
```bash
npm install -g pm2
```

2. Запустите бота через PM2:
```bash
npm run pm2:start
```

3. Сохраните конфигурацию PM2 (чтобы бот запускался после перезагрузки сервера):
```bash
npm run pm2:save
npm run pm2:startup
```

4. Полезные команды для управления ботом:
```bash
npm run pm2:status    # Проверить статус бота
npm run pm2:logs      # Просмотреть логи
npm run pm2:restart   # Перезапустить бота
npm run pm2:stop      # Остановить бота
npm run pm2:delete    # Удалить бота из PM2
```

Или используйте команды PM2 напрямую:
```bash
pm2 status
pm2 logs ies-crm-bot
pm2 restart ies-crm-bot
pm2 stop ies-crm-bot
pm2 delete ies-crm-bot
```

## Использование

1. Запустите бота
2. Найдите вашего бота в Telegram
3. Отправьте команду `/start`
4. Введите email и пароль для авторизации
5. Используйте меню для работы с товарами

## Структура проекта

```
bot/
├── api/              # API клиенты для работы с Laravel
│   ├── client.js     # Базовый HTTP клиент
│   ├── auth.js       # API для авторизации
│   └── goods.js      # API для работы с товарами
├── handlers/         # Обработчики команд и сообщений
│   ├── auth.js       # Обработка авторизации
│   └── goods.js      # Обработка работы с товарами
├── keyboards/        # Клавиатуры для меню
│   └── menu.js       # Меню бота
├── config.js         # Конфигурация
├── index.js          # Главный файл бота
└── package.json      # Зависимости проекта
```

## Примечания

- Токены пользователей хранятся в памяти (Map). В продакшене рекомендуется использовать базу данных.
- Состояния пользователей также хранятся в памяти. При перезапуске бота они будут потеряны.
- Для продакшена рекомендуется использовать webhook вместо polling.

## Разработка

Бот использует библиотеку [Telegraf](https://telegraf.js.org/) для работы с Telegram Bot API.

Все точки с запятой добавлены в соответствии с требованиями проекта.

